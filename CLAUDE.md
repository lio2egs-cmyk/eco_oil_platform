# CLAUDE.md — אקו אויל: מערכת הניהול, הפורטלים, אקו דיפו והאתר

מסמך הבסיס היחיד. נטען אוטומטית בכל שיחה. קצר בכוונה: הפרטים חיים בזיכרון (אינדקס MEMORY.md) ובספר החוקים. עודכן 16/09/2026.

## 1. עם מי אני עובדת
לימור טואג, מנהלת תפעול באקו אויל חץ וירומטל בע"מ (חיפה). לא-טכנית: לא קוראת קוד, קוראת תוצאות. המנכ"ל יואב טואג מאשר כל דבר שפונה ללקוח. יעל = עובדת המשרד שמזינה דרך הטפסים במחשב shtifot (DESKTOP-HQA82VV). לימור עובדת רק בתוך Claude Desktop על Windows 11.

## 2. איך מדברים (החוקים המלאים בזיכרון: feedback_working_style, feedback_explanation_style, feedback_declare_certainty)
- עברית פשוטה. בלי בלוקי קוד אלא אם ביקשה. תוצאות, לא מימוש.
- הודעות קצרות: הודעה = שלב אחד או שאלה אחת. לא מגילות. הסבר מנגנון = צעדים ממוספרים, מסלול אחד, מה לא משתנה, אומדן.
- כל טענה מסומנת מאומת / השערה. לא ממציאים סיפור למלא חור.
- אימות-קודם: לפני כל שאלה ללימור ממצים את המקורות (תיקיות, תעודות PDF, וואטסאפ, מיילים, git, הקובץ החי) ומגיעים עם תשובה והמלצה.
- לא לחנוף. לומר כשהיא טועה. לשאול לפני הכרעה ארכיטקטונית או פונה-ללקוח, עם 2-4 אפשרויות ומומלצת מסומנת.
- מבנה טבלאי מציגים כקובץ אקסל אמיתי, לא כטקסט. דוחות = טבלה עם גבולות, RTL.
- אותה פעולה = אותו סמל באותו מקום בכל מסך (עקביות UI).
- ביסודיות ולא קיצורי דרך; לא להכתיר workaround כהצלחה.

## 3. לפני עבודה על אקו דיפו או פורטל אויל
לקרוא את ספר החוקים: `C:\Users\Office\.claude\projects\C--eco-oil-platform-git\memory\project_ecodepot_rules.md` (חוקים 1-76: כתיבה לקובץ החי, חיוב, ראיות, מסופונים, תיוק, פורטל אויל). שינוי חוק = רק בהנחיה מפורשת של לימור/יואב. ואז את קובץ "מצב נוכחי" של התחום מהאינדקס.

## 4. מפת המערכת (מה נמצא איפה)
| רכיב | היכן | הערות |
|---|---|---|
| פורטלי לקוחות (Flask) | `src/app/` בריפו זה; חי ב-Railway (Postgres) | depot.eco-oil.co.il, portal.eco-oil.co.il; כניסה בקישור קסם; נפרס אוטומטית מענף `dev` |
| אתר השיווק | `website/` בריפו זה; חי ב-Netlify | נפרס אוטומטית מ-`dev` (base dir website/); טפסים דרך Netlify Forms |
| גשרים וסבבים (bridge/push) | סקריפטי `ecooil_*` / `depot_*` בשורש הריפו | רצים במשימות מתוזמנות (מחשב יעל / לימור) ודוחפים לענן |
| הקובץ החי של הדיפו | `O:\SHTIFOT\מערכת ניהול אקו דיפו\EcoDepot.xlsx` | O: קיים רק אצל לימור; בכל מחשב אחר ובכל שיחה עם אנשים = "הכונן המרכזי" / Z: |
| טפסי הדסקטופ (EXE) | מקור `C:\eco_depot_form*`; פריסה ל-`O:\...\אפליקציית טופס\` | איזוטנק, רואדטנקר, תעודות, דוחות עובדים |
| סקריפטים תפעוליים + גיבויים | `C:\for_eco-depot\` (scripts\, EcoDepot_BACKUP_pre_*) | כל גיבוי וקובץ עבודה של הדיפו רק כאן |
| קבצי עבודה של הפורטלים | `C:\eco_oil_portal\` | מוקאפים, רשימות, תבניות |
| מסופוני שטח | PWA בפורטל + גשר במחשב יעל | ראו זיכרון project_ecodepot_field_terminals |
| מסד חומרים ומחירון | `מחירון חדש.xlsx` על הכונן המרכזי, גיליון "מסד חומרים" | מקור האמת לקטלוג; Power Query אל EcoDepot |
| ריכוז אקו אויל | קובץ הריכוז אצל לימור (רישום התעודות) | הגשר קורא ממנו כל שעה |

מסמכים ישנים (הנחיות מאי 2026, פריסת האתר, העברה מהנייד): `docs/archive/`. לא נטענים; לקרוא רק כשנשאלים על העבר.

## 5. קווים אדומים
- קבצי פרודקשן (הקובץ החי, קבצי לקוחות, תיקיות על הכונן המרכזי): לא דורסים, לא מוחקים, לא מזיזים. כתיבה רק בסקריפט אחד: נעילה → גיבוי ל-C: → אימות שורה → כתיבה → חישוב → סריקת שגיאות → שמירה מאומתת. מחוץ לתיקיות שלנו הכונן המרכזי קריאה-בלבד.
- אקסל: `DispatchEx` בלבד, לעולם לא `Dispatch`/`GetActiveObject` (סגר את הריכוז של לימור פעמיים). לא `taskkill EXCEL` גורף. COM דרך `py` (פייתון גלובלי), לא ה-venv. תאריכים כמספר סריאלי. openpyxl לקריאה בלבד.
- git: עובדים על `dev`. `master` ישן ולא בשימוש. push = פריסה חיה (Netlify + Railway) ⇒ רק אחרי שלימור ראתה ואישרה. אין `--force`, אין `--no-verify`. סודות רק ב-`.env`.
- וואטסאפ וג'ימייל של הדיפו: קריאה בלבד. אין שליחה, אין מחיקה, אין הגדרות.
- כסף, חתימות, הרשאות, סיסמאות, תנאי שימוש: לימור בעצמה.
- אין הפקה-מחדש של דוח שנמסר ללקוח בלי אישור מפורש של לימור (חוק 58א).
- קבצי עבודה זמניים לא לשולחן העבודה; לתיקיית התחום ב-C: או ל-scratchpad.

## 6. סביבה וכלים
- פורטל מקומי: `venv\Scripts\python.exe run.py` (5000, בלי auto-reload) או דרך `.claude/launch.json` ("portal"). SQLite ב-`data/app.db`; מנהל מקומי admin/changeme123 (מקומי בלבד).
- עברית בקונסול = ג'יבריש (cp1255). לאמת דרך קובץ UTF-8 או הדפדפן, לעולם לא מהטרמינל.
- הוק הזיכרון בולע `\f` בנתיבים (C:\for → C:or). אחרי כתיבת זיכרון עם נתיב כזה לבדוק, או לכתוב `C:/for_...`.
- מחשב יעל: אחרי אתחול המשימות לא עולות עד התחברות; גשר עם דופק ב-logs\bridge_heartbeat.txt.
- פרוטוקול הבלשית (חקירת שורות/מכלים): טריגר "הפעילי את פרוטוקול הבלשית" + רשימת שורות.

## 7. סקילים
סקילים (נטענים לשיחה, אני לובשת את התפקיד): `ecodepot` (הקובץ החי + דוחות), `materials` (קטלוג חומרים), `portals` (הפורטלים), `field-terminals` (טאבלטים + גשר יעל), `detective` (פרוטוקול הבלשית). סוכנים (עובדים לבד ומחזירים דוח, קריאה בלבד): `evidence-search` (ראיות למכל/שורה), `state-check` (מה באמת רץ). לפני שימוש בסקיל לאמת מצב בקוד וב-git; הסקילים מתארים מבנה וכללים, הזיכרון מתאר מצב.

## 8. אתר השיווק (website/)
אתר סטטי, חי ב-Netlify מענף `dev` (base directory website/). דפים: index, about, services-offered, industrial-consulting, eco-depot, partners, export-of-waste, types-of-waste-water; טפסים דרך Netlify Forms; `_redirects` מנקה סיומות .html. **כל קובץ בתוך website/ מתפרסם לציבור** — לכן אין שם הערות פנימיות (CLAUDE.md, memory) ואין מסמכי עבודה; הערות על האתר נשארות כאן. עיצוב: קיימת שפת עיצוב ב-`style.css` + css לכל דף; לא משנים ביוזמתי, רכיב חדש באותה שפה; אחרי שינוי פריסה להראות ללימור איך לבדוק במחשב/טאבלט/נייד. שני דפי התעודות (cleaning_certificate.html אנגלית, release_certificate.html עברית) הם תבניות מאפריל 2026 למילוי ידני; התעודות התפעוליות מופקות היום מהקובץ החי. פירוט החלטות העיצוב: `docs/archive/website_CLAUDE_full_2026-04.md`.
