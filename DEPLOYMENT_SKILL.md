# DEPLOYMENT SKILL — העלאת אתר אקו אויל לרשת מ-א' עד ת'

> **לשימוש Claude Code על המחשב המרכזי של אקו אויל.**
> מדריך מלא, מקיף, צעד-אחר-צעד, להעלאת אתר סטטי לרשת.
> נכתב במיוחד עבור לימור — שאין לה ניסיון קודם בהעלאת אתרים, וזו הפעם הראשונה.

---

## 📖 איך להשתמש בסקיל הזה

**עבור Claude:** קראי את כל הסקיל לפני שאת מתחילה להנחות את לימור. אל תדלגי על שלבים. אל תניחי שהיא יודעת מונחים — תסבירי כל מונח בפעם הראשונה שמופיע. בכל שלב:
1. תסבירי **מה** אתן עומדות לעשות.
2. תסבירי **למה** זה צריך לקרות.
3. תני **הוראה אחת** ספציפית.
4. **תחכי לאישור.**
5. רק אחרי אישור — שלב הבא.

**עבור לימור:** הסקיל הזה הוא המסלול הבטוח שלך. את לא צריכה לזכור את כולו — Claude תוביל אותך. אבל אם בא לך לקרוא בהשקט מראש — בבקשה.

---

# 📚 מדריך התוכן

[חלק 1: מילון מונחים](#חלק-1-מילון-מונחים) — חובה לקרוא ראשון
[חלק 2: התמונה הגדולה](#חלק-2-התמונה-הגדולה) — מה זה בעצם "להעלות אתר"
[חלק 3: הארכיטקטורה שבחרנו](#חלק-3-הארכיטקטורה-שבחרנו) — איזה שירותים נשתמש ולמה
[חלק 4: רשימת בדיקה לפני שמתחילים](#חלק-4-רשימת-בדיקה-לפני-שמתחילים)
[חלק 5: שלב הקדם — איפיון הטפסים](#חלק-5-שלב-הקדם--איפיון-הטפסים)
[חלק 6: שלב 1 — יצירת חשבון Netlify](#חלק-6-שלב-1--יצירת-חשבון-netlify)
[חלק 7: שלב 2 — חיבור הגיט לNetlify (פריסה אוטומטית)](#חלק-7-שלב-2--חיבור-הגיט-לnetlify)
[חלק 8: שלב 3 — חיבור הדומיין eco-oil.co.il](#חלק-8-שלב-3--חיבור-הדומיין)
[חלק 9: שלב 4 — הפעלת HTTPS (המנעול הירוק)](#חלק-9-שלב-4--הפעלת-https)
[חלק 10: שלב 5 — חיבור הטפסים](#חלק-10-שלב-5--חיבור-הטפסים)
[חלק 11: שלב 6 — בדיקות אחרי העלאה](#חלק-11-שלב-6--בדיקות-אחרי-עלייה)
[חלק 12: שלב 7 — מערכות עזר](#חלק-12-שלב-7--מערכות-עזר)
[חלק 13: תחזוקה שוטפת](#חלק-13-תחזוקה-שוטפת)
[חלק 14: בעיות נפוצות וטיפולים](#חלק-14-בעיות-נפוצות-וטיפולים)
[חלק 15: מה הלאה — אחרי שהאתר באוויר](#חלק-15-מה-הלאה)

---

# חלק 1: מילון מונחים

לפני שאת מתחילה לעשות משהו — חייבת להבין את המילים. אסור לדלג. כל הפעולות בהמשך משתמשות בהן.

## 🌐 דומיין (Domain)
זה ה"כתובת" של האתר באינטרנט. למשל `eco-oil.co.il`. כשמישהו מקליד את הכתובת הזו בדפדפן — הוא מגיע לאתר שלך. **הדומיין כבר רכוש שלך** — רשום על שם החברה דרך חברת *Domain The Net* (אקסקלוסיב נטוורקס).

## 🏠 הוסטינג (Hosting / אחסון אתרים)
זה השירות שמאחסן את הקבצים של האתר על מחשבים שלהם (שנקראים "שרתים") שמחוברים תמיד לאינטרנט. כשמישהו מבקר באתר — הדפדפן שלו "מבקש" את הקבצים מהשרת של חברת ההוסטינג. **אנחנו עוד לא מחוברים להוסטינג — את זה נעשה במדריך הזה.**

## 📡 DNS (Domain Name System)
מערכת שמתרגמת בין שם דומיין (`eco-oil.co.il`) לבין כתובת השרת המספרית (כמו `185.x.x.x`). כל פעם שמישהו פותח את האתר — הדפדפן שואל את ה-DNS "איפה אקו אויל?" וה-DNS עונה לו. **את הגדרות ה-DNS נשנה אצל Domain The Net כדי להפנות את הדומיין שלנו אל ההוסטינג שנבחר.**

## 🔒 HTTPS / SSL / TLS
פרוטוקול אבטחה. כשרואים מנעול ירוק/אפור ליד כתובת האתר בדפדפן — זה אומר שהאתר מאובטח בHTTPS. **חובה לכל אתר רציני.** ההוסטינג שנבחר ייצר את התעודה הזו אוטומטית בחינם.

## 🔄 CDN (Content Delivery Network)
רשת של שרתים בעולם שמאחסנים העתקים של האתר שלך, כדי שלקוח מארה"ב יקבל את האתר מסיאטל ולקוח מאירופה יקבל אותו מאמסטרדם — מהיר. **ההוסטינג שנבחר כולל CDN בחינם.**

## 📂 גיט / GitHub
מערכת לשמירת קוד עם היסטוריה. הפרויקט שלנו כבר נמצא ב-GitHub (`github.com/lio2egs-cmyk/eco_oil_platform`). **הוסטינג מודרני יודע "להקשיב" ל-GitHub** — בכל פעם שמשנים משהו ושומרים בגיט, האתר באוויר מתעדכן אוטומטית. זה הכלי החזק ביותר שיש לנו.

## 🚀 פריסה (Deployment / Deploy)
הפעולה של העברת האתר מהמחשב המקומי אל ההוסטינג. אחרי "פריסה ראשונה" — האתר באוויר. אחרי כל שינוי שנפרוס — הגרסה באוויר מתעדכנת.

## 📧 טפסים (Forms)
הטפסים שבדפי האתר (יצירת קשר). כרגע הם **לא שולחים שום פנייה לאף מקום** — הם מציגים הודעת הצלחה אבל הפנייה לא מגיעה לאף תיבת מייל. **חלק מהמדריך הזה הוא לחבר אותם נכון.**

## 🌍 Netlify
חברת אחסון אתרים סטטיים. **השירות המומלץ שלנו לאתר.** חינם לשימוש שלנו (יש להם חבילה חינמית נדיבה), מהיר, אמין, וכולל טפסים מובנים בלי שצריך שירות צד שלישי.

## 🌍 דומיין דה נט / Domain The Net
חברת רישום הדומיינים שלנו. כתובת: `domainthenet.com`. שם נכנסים לחשבון לעדכן הגדרות DNS. **לא נחליף את חברת הרישום** — רק נשנה הגדרות.

---

# חלק 2: התמונה הגדולה

לפני שמתחילים — תני ללימור להבין במשפט-שניים מה הולך לקרות:

> "האתר שלך כרגע יושב כקבצים על המחשב הזה. כדי שכולם בעולם יוכלו לראות אותו ב-`eco-oil.co.il`, אנחנו צריכים:
> 1. לבחור חברה שתאחסן את הקבצים על שרתים שלהם (Netlify).
> 2. להעלות את הקבצים אליהם.
> 3. להגיד לחברת הדומיין שלך (Domain The Net) — 'מי שמחפש את eco-oil.co.il, תשלח אותו לנטליפיי'.
> 4. לוודא שהמנעול הירוק (HTTPS) פועל.
> 5. לוודא שהטפסים מגיעים אליך לאימייל.
>
> זהו. זה כל הסיפור. אנחנו פשוט נבצע אותו בהדרגה ובזהירות."

---

# חלק 3: הארכיטקטורה שבחרנו

## למה Netlify?

יש כמה אפשרויות הוסטינג מודרניות. בחרתי **Netlify** עבור לימור כי:

1. **ממשק פשוט מאוד** — בנוי לאנשים לא טכניים.
2. **טפסים מובנים בחינם** — לא צריך שירות צד שלישי לחיבור הטפסים.
3. **חיבור אוטומטי לגיט** — בכל commit החדש האתר מתעדכן אוטומטית.
4. **HTTPS אוטומטי** — בלי תשלום, בלי כאב ראש.
5. **CDN גלובלי** — האתר מהיר בכל מקום.
6. **חבילה חינמית נדיבה** — לאתר בגודל ובתנועה של אקו אויל זה ייתן 100% חינם.
7. **מחזיק רוב הצרכים שלנו לכל החיים** — גם כשנגדל.

## מה לא בחרנו ולמה

- **Cloudflare Pages** — מצוין, אבל אין טפסים מובנים. נצטרך לחבר Formspree בנפרד = מורכבות נוספת.
- **Vercel** — מצוין לפרויקטים של Next.js, אבל פחות מתאים לאתרים סטטיים פשוטים.
- **GitHub Pages** — חינמי וטוב, אבל אין טפסים, ולא טוב למסחרי.
- **הוסטינג ישראלי קלאסי (כמו של Domain The Net עצמם)** — יותר יקר, יותר איטי, ממשק מסורבל. לא מומלץ.

## הארכיטקטורה הסופית

```
┌─────────────────┐
│ Visitor         │ ← מבקר באינטרנט
└────────┬────────┘
         │ "אני רוצה את eco-oil.co.il"
         ▼
┌─────────────────┐
│ DNS (DomainNet) │ ← מתרגם את השם לשרת
└────────┬────────┘
         │ "תפנה ל-Netlify"
         ▼
┌─────────────────┐
│ Netlify CDN     │ ← מאחסן את האתר ומגיש אותו
└────────┬────────┘
         │
         │ ⇅ הצורה שמתחת היא רק סנכרון
         │
┌─────────────────┐
│ GitHub repo     │ ← מקור האמת של הקוד
└─────────────────┘
```

---

# חלק 4: רשימת בדיקה לפני שמתחילים

לפני שאתן מתחילות את התהליך — בדקי את הפריטים האלה:

## ✅ דרישות מקדימות שצריכות להיות מוכנות

1. **גיט מותקן במחשב העבודה?**
   - בדיקה: `git --version` בטרמינל.
   - אם לא — לבקש מאיש המחשבים להתקין.

2. **חשבון GitHub פעיל ומחובר?**
   - הפרויקט נמצא ב-`github.com/lio2egs-cmyk/eco_oil_platform`.
   - ודאי שאת יכולה לעשות `git pull` ו-`git push` ללא בעיה.
   - **אם אין הרשאות במחשב העבודה** — צריך להגדיר git credentials. שאלי את לימור על שם המשתמש שלה ב-GitHub ועל "Personal Access Token" אם יש לה.

3. **דומיין רשום על שם החברה?**
   - **כן** — `eco-oil.co.il`, רשום ב-Domain The Net.
   - לימור צריכה גישה לחשבון Domain The Net (שם משתמש + סיסמה).

4. **גישת אדמין למייל של החברה?**
   - לימור צריכה גישה לאימייל הראשי של החברה (יואב המנכ"ל) כדי לקבל הודעות אימות.
   - לחלופין: גישה לאימייל הפרטי שלה כדי לקבל אישורים בשלב הראשון.

5. **כל 8 דפי האתר מאושרים?**
   - לפי הזיכרון של הפרויקט, כן — אושרו ב-2026-04-30 ע"י המנכ"ל.

6. **כל לוגואי הלקוחות שאסור להציג — הוסרו?**
   - לפי 2026-05-07 — לוגו טנקו הוסר. ודאי לפני העלאה שאין לוגואים אחרים שלא קיבלו אישור.

## 🛑 דברים שעוד לא הוחלטו ושצריכים החלטה לפני התחלה

- **מי יקבל את הפניות מהטופס?** האם המייל של יואב? האם של לימור? האם שניהם? **שאלי את לימור.**
- **שם החשבון ב-Netlify** — יהיה על שם החברה ("Eco Oil") או על שם פרטי? **שאלי את לימור.**
- **האם להפעיל מצב תחזוקה / "באנר חוסם" כשמעלים פעם ראשונה** למקרה של תקלה? לא חובה, אבל אופציה.

---

# חלק 5: שלב הקדם — איפיון הטפסים

לפני שמעלים את האתר — צריך לתקן את הטפסים כדי שיעבדו עם Netlify Forms. כרגע (לפי קריאת הקוד) הטפסים *מציגים* הודעת הצלחה אבל לא *שולחים* פנייה לאף מקום. צריך לתקן את זה.

## מה צריך לשנות בקבצים

### בקובץ `index.html` ובקובץ `eco-depot.html`:
על תגית הטופס לכלול:
- `name="contact-form"` (שם ייחודי לכל טופס)
- `method="POST"`
- `data-netlify="true"`
- `netlify-honeypot="bot-field"` (להגנה מספאם — שדה נסתר שבוטים ממלאים)
- שדה `<input type="hidden" name="form-name" value="contact-form">` בתוך הטופס

### בקובץ `script.js`:
לבטל את ה-`e.preventDefault()` בטיפוס שולח טפסים, או לאפשר שליחה אמיתית במקביל לאנימציה. הצעה: לתת לטופס להישלח באמת ל-Netlify, ולהציג את הודעת ההצלחה רק אחרי שהשרת אישר קבלה.

## ⚠️ הוראה לClaude

**אל תבצעי את השינויים האלה לבד.**

תני ללימור הסבר במשפט-שניים, תציעי "לבצע את השינויים?" — וחכי לאישור. כשהיא מאשרת — בצעי, ותראי לה את התוצאה לפני שעוברת הלאה.

הסיבה: הטפסים האלה הם הקול של החברה לקהל. כל באג כאן = פניות שלא מגיעות.

---

# חלק 6: שלב 1 — יצירת חשבון Netlify

## מטרה
לפתוח חשבון בשירות Netlify על שם חברת אקו אויל.

## הוראות צעד-אחר-צעד ללימור

1. **פתחי דפדפן** (כרום מומלץ) ולכי לכתובת: `https://www.netlify.com`
2. **לחצי "Sign up"** (להירשם) — בדרך כלל בפינה הימנית-עליונה.
3. **תופיע אופציה להירשם דרך GitHub.** לחצי על זה — **חשוב מאוד**, כי זה יחבר את חשבון Netlify לחשבון GitHub שלנו.
4. תועברי לדף של GitHub שיבקש אישור הרשאות. **לחצי "Authorize Netlify"** (לאשר את Netlify לקרוא את המאגרים שלך).
5. **תחזרי לNetlify.** הם ישאלו שאלות קצרות (איזה סוג עבודה את עושה, אם זה לעבודה או לפרויקט אישי). תעני **בכנות** — את עובדת על אתר חברה קטנה, לא טכנית.
6. **תקבלי דשבורד ריק.** עכשיו יש לך חשבון.

## 🛑 קווים אדומים לClaude
- **אל תכניסי שם משתמש או סיסמה** במקום לימור. כל הזדהות היא בידיים שלה בלבד.
- **אל תאשרי הרשאות ל-OAuth** במקום לימור.
- **אל תבחרי תוכניות תשלום (paid plans).** החבילה החינמית מספיקה עבורנו — ודאי שלימור בוחרת "Free" / "Starter".

## אישור לפני המשך
לפני שעוברים לשלב 2 — לימור צריכה לאשר ש:
- ✅ נכנסה לדשבורד של Netlify.
- ✅ ראתה הודעה שמאשרת שהחשבון נוצר.

---

# חלק 7: שלב 2 — חיבור הגיט לNetlify (פריסה אוטומטית)

## מטרה
להגיד לNetlify: "המאגר ב-GitHub שלי הוא `github.com/lio2egs-cmyk/eco_oil_platform`. תעקבי אחריו, בכל פעם שאני שולחת שינוי — תפרסי אותו."

## הוראות צעד-אחר-צעד

### א. וידוא שהאתר מסונכרן בגיט
לפני שמחברים את Netlify, חשוב שכל השינויים האחרונים נדחפו לגיט (`git push`).
- בדקי במחשב העבודה: `git status` — אם יש שינויים שלא נדחפו, צריך לעשות `git add`, `git commit`, `git push`.
- **שאלי את לימור לפני שאת עושה commit/push** — תני לה לראות מה השינויים.

### ב. יצירת אתר חדש בNetlify

1. **בדשבורד של Netlify**, מצאי כפתור גדול **"Add new site"** או **"Import from Git"**.
2. **בחרי "GitHub"** כמקור.
3. **תקבלי רשימה של מאגרי הגיט** שיש לך גישה אליהם. בחרי **`eco_oil_platform`**.
4. **הגדרות בנייה:**
   - **Branch to deploy:** `master` (זה הענף הראשי).
   - **Base directory:** `website` (חשוב מאוד! האתר נמצא בתת-תיקייה `website/`, לא בשורש המאגר).
   - **Build command:** השאירי **ריק** (האתר שלנו סטטי, אין צורך בבנייה).
   - **Publish directory:** ריק או `.` (כי כבר הגדרנו את `website/` כ-Base directory).
5. **לחצי "Deploy site"**.
6. תהליך הפריסה הראשון יתחיל. ייקח כדקה.

### ג. בדיקה שהפריסה הצליחה

1. בדשבורד תראי הודעה ירוקה "Site is live" או דומה.
2. Netlify ייתן לך כתובת זמנית כמו `random-name-xxxx.netlify.app`.
3. **פתחי את הכתובת בדפדפן.** את אמורה לראות את האתר שלנו!
4. **תעברי על כל 8 הדפים** ותוודאי שהכל עובד: שהתמונות נטענות, שהקישורים לא שבורים, שעיצוב שמור.

## 🛑 קווים אדומים לClaude
- **אם משהו לא נטען נכון** (תמונה חסרה, כתב לא נכון) — **עצרי**. תני ללימור לראות, לתאר את הבעיה, ואז ניגש לתקן בקוד.
- **אסור** "להעלים" בעיות — דווחי על כל אזהרה (warning) ב-build log.

## אישור לפני המשך
- ✅ האתר באוויר על כתובת זמנית.
- ✅ כל 8 הדפים נראים תקינים.
- ✅ הטפסים נראים תקינים (עוד לא בודקים שליחה).

---

# חלק 8: שלב 3 — חיבור הדומיין eco-oil.co.il

## מטרה
לעבור מהכתובת הזמנית של Netlify (`random-xxx.netlify.app`) לכתובת האמיתית `eco-oil.co.il`.

## הוראות צעד-אחר-צעד

### א. הוספת הדומיין בדשבורד Netlify

1. **בדשבורד של האתר ב-Netlify**, לכי ל**"Domain settings"** או **"Domain management"**.
2. **לחצי "Add a custom domain"** (להוסיף דומיין מותאם).
3. **הקלידי:** `eco-oil.co.il`
4. Netlify יבדוק שהדומיין באמת לא בשימוש בחשבון אחר. אם יש בעיה — דווחי ללימור.
5. **לחצי "Verify"** ואז **"Yes, add domain"**.
6. תראי את הדומיין מופיע ברשימה עם הודעה: "Awaiting external DNS" או "Pending DNS configuration".

### ב. הוספת גם הגרסה עם www

חשוב להוסיף גם `www.eco-oil.co.il` ככתובת חלופית:
1. שוב **"Add a custom domain"**.
2. הפעם הקלידי: `www.eco-oil.co.il`.
3. Netlify יבחר אוטומטית אחת מהן כ"primary" — לא קריטי איזו, ההמלצה היא **בלי www**.

### ג. שינוי DNS ב-Domain The Net

זה החלק הקריטי. אנחנו אומרים ל-Domain The Net: "מי שמחפש את eco-oil.co.il — תפנה אותו ל-Netlify".

1. **התחברי לחשבון Domain The Net** ב-`https://domainthenet.com` (תזדקקי לשם משתמש וסיסמה).
2. **מצאי את הדומיין** `eco-oil.co.il` בחשבון.
3. **חפשי את האפשרות "DNS Management"** או "ניהול DNS" או "אזור DNS".
4. **תראי טבלה** של רשומות DNS קיימות (A records, MX records, וכו').

### ד. הרשומות לעדכון

יש שתי אופציות. **בחרי אופציה א'** (פשוטה יותר, מספיקה לנו):

#### אופציה א' — שינוי רשומות A ו-CNAME (מומלץ)

**רשומות שצריך להוסיף או לעדכן:**

| Type | Name | Value | TTL |
|------|------|-------|-----|
| A | @ | 75.2.60.5 | 3600 |
| CNAME | www | apex-loadbalancer.netlify.com | 3600 |

הסבר:
- `@` הוא הדומיין הראשי `eco-oil.co.il` (בלי www).
- `75.2.60.5` הוא ה-IP של load balancer של Netlify (זה מספר שNetlify משתמשים בו לכל הלקוחות).
- `www` יפנה ל-CNAME של Netlify, שמטפל בעצמו בכל היתר.
- TTL = "Time To Live" = כמה שניות שרתי DNS בעולם יזכרו את ההגדרות. 3600 = שעה. סביר.

**מחקי כל רשומת A או CNAME ישנה שמפנה ליעד אחר** (למשל הוסטינג ישן).

⚠️ **אל תיגעי ברשומות MX** (אלה לאימייל) — נטפל בהן בשלב נפרד אחר כך.

#### אופציה ב' — שינוי Nameservers (לא מומלץ עכשיו)
זו אופציה מתקדמת שמעבירה את כל ניהול ה-DNS ל-Netlify. **דלגי עליה כעת** — אופציה א' מספיקה ופחות מסוכנת.

### ה. אישור השינויים

1. **שמרי את השינויים** ב-Domain The Net.
2. **חזרי לדשבורד Netlify** והמתיני 5-10 דקות.
3. עדכני את הדף — תראי שהסטטוס משתנה מ"Awaiting" ל"Netlify DNS" או "DNS verification successful".

### ו. המתנה לפרסום עולמי (DNS Propagation)

הדבר הזה יכול לקחת **מ-15 דקות עד 48 שעות** (בדרך כלל בתוך שעה זה עובד, לפעמים יותר). בזמן הזה:
- חלק מהאנשים בעולם יראו את האתר החדש.
- חלק עדיין יראו את הישן (או "page not found").

זה **נורמלי** — אין מה לתקן, רק להמתין.

### ז. בדיקה

1. אחרי 30 דקות, נסי לפתוח `https://eco-oil.co.il` בדפדפן.
2. אם רואים את האתר — מעולה.
3. אם רואים שגיאה או דף ריק — חכי עוד שעה.
4. כדי לבדוק "מה העולם רואה" — היכנסי ל-`https://www.whatsmydns.net`, הקלידי `eco-oil.co.il`, ובחרי "A". אם רוב התוצאות מציגות את ה-IP של Netlify (`75.2.60.5`) — הפרסום הצליח.

## 🛑 קווים אדומים לClaude
- **אסור לגעת ברשומות MX של הדומיין** בלי לבקש אישור — זה ישבור את האימייל של החברה!
- **אסור למחוק את כל הרשומות הישנות** — רק את אלה שמפנות לאתר. רשומות אחרות (TXT, MX, וכו') להשאיר.
- **אם לימור לא בטוחה איך להיכנס לחשבון Domain The Net** — תני לה אופציה לקבל עזרה מאיש המחשבים.

## אישור לפני המשך
- ✅ הדומיין נוסף ב-Netlify.
- ✅ רשומות DNS עודכנו ב-Domain The Net.
- ✅ `eco-oil.co.il` נטען בדפדפן ומראה את האתר (אחרי המתנה).

---

# חלק 9: שלב 4 — הפעלת HTTPS (המנעול הירוק)

## מטרה
שכל מי שיגיע ל-`http://eco-oil.co.il` יועבר אוטומטית ל-`https://eco-oil.co.il` — עם תעודת אבטחה תקפה.

## הוראות

זה הכי קל — **Netlify יעשה את זה אוטומטית** ברגע שהדומיין מחובר נכון.

1. **בדשבורד Netlify**, לכי ל-**Domain Settings → HTTPS**.
2. תראי הודעה: "Verify DNS configuration" → אם זה ירוק = הכל מוכן.
3. **לחצי "Provision certificate"** (לייצר תעודה).
4. Netlify ידבר עם Let's Encrypt (חברה שמייצרת תעודות SSL בחינם) ויקבל תעודה לדומיין שלך.
5. תהליך זה לוקח 1-5 דקות.
6. אחרי שזה ירוק — כל גישה ל-`http://` תועבר אוטומטית ל-`https://`.

## בדיקה
- פתחי `https://eco-oil.co.il` בדפדפן.
- בכתובת — מנעול אפור או ירוק. **לחצי עליו**. בחלונית שתיפתח — את אמורה לראות "Connection is secure" / "Certificate valid".
- נסי גם את `http://eco-oil.co.il` (בלי s) — את אמורה להיות מועברת אוטומטית ל-https.

## אישור לפני המשך
- ✅ מנעול ירוק/אפור מופיע בכתובת.
- ✅ הפניית http → https עובדת.

---

# חלק 10: שלב 5 — חיבור הטפסים

## מטרה
שכל פנייה דרך טופס יצירת קשר תגיע למייל אמיתי של מישהו בחברה.

## הוראות

### א. וידוא שהשינויים בקוד מ-"חלק 5" הוכנסו וND-נדחפו לגיט
אם עוד לא — צריך לבצע את השינויים בקוד עכשיו. תני ללימור לראות, אישור, ובצעי commit + push.

### ב. בדיקה ראשונה ש-Netlify מזהה את הטפסים

1. **לכי לדשבורד Netlify** של האתר → **Forms**.
2. **לחצי "Add a form"** או "Configure forms" (תלוי בגרסה).
3. **בעצם — צריכה להופיע אוטומטית רשימה** של הטפסים שמופיעים בקוד עם `data-netlify="true"`. אמורות להופיע שתיים: `contact-form` (מהדף הראשי) ו-`depot-contact-form` (מאקו דיפו).
4. **אם לא מופיעות** — זה אומר שהשינויים בקוד לא נדחפו, או שהפריסה לא הצליחה. דווחי.

### ג. הגדרת הודעות אימייל

1. **לכי לטופס "contact-form" → Notifications**.
2. **Add notification → Email notification**.
3. **הקלידי את כתובת המייל** של מי שצריך לקבל את הפניות. **שאלי את לימור** — האם זו תיבה של יואב? לימור? של החברה?
4. שמרי.
5. **חזרי על אותו תהליך** עבור הטופס `depot-contact-form` (יכול להגיע לאותה תיבה או לתיבה נפרדת — שאלי).

### ד. בדיקת שליחה ראשונה

1. פתחי את `https://eco-oil.co.il` (האתר החי, לא הסביבה הזמנית).
2. לכי לטופס יצירת קשר.
3. **מלאי בדיקה אמיתית** עם פרטים שלך, ובהודעה כתבי משהו כמו: "בדיקה — לימור 7/5/26".
4. שלחי.
5. ודאי שהודעת ההצלחה מופיעה בדף.
6. **בדקי בתיבת המייל המוגדרת** — האם הפנייה הגיעה?
7. ודאי שגם **שדות ההיברו** עברו נכון (לא הופיעו תווים מוזרים).
8. **חזרי על הבדיקה** מטופס אקו דיפו.

### ה. הגנה מספאם

ב-Netlify Forms יש מנגנונים מובנים, אבל מומלץ:
1. **בדף Forms → Settings**, וודאי ש"Honeypot field" מוגדר ל-`bot-field` (זה מה שהגדרנו בקוד).
2. **לא להפעיל reCAPTCHA כברירת מחדל** — זה מסבך את הטפסים. נחזור אליו אם נראה הרבה ספאם בפועל.

## 🛑 קווים אדומים לClaude
- **אסור לבדוק טפסים בעזרת מיילים פיקטיביים** או להזין מייל לא של לימור — זה דורש את אישורה.
- **אם הבדיקה לא הצליחה** — אל תנסי "תיקונים" אקראיים. תני ללימור לתאר במילים מה ראתה, ונחקור יחד.

## אישור לפני המשך
- ✅ פנייה מטופס הראשי מגיעה למייל.
- ✅ פנייה מטופס אקו דיפו מגיעה למייל.
- ✅ עברית עברה תקין.

---

# חלק 11: שלב 6 — בדיקות אחרי עלייה

## רשימת בדיקות הכרחית

עברי על כל אחת ואשרי ידנית:

### 📱 בדיקות בדפדפנים שונים
- [ ] Chrome (כל גרסה עדכנית)
- [ ] Edge (כי הרבה משתמשים בו בישראל)
- [ ] Firefox
- [ ] Safari (אם יש לך iPhone או Mac)
- [ ] Samsung Internet (אם יש לך אנדרואיד)

### 🖥️ בדיקות גודלי מסך
פתחי את האתר וב-Chrome → קליק ימני → Inspect → אייקון של טאבלט/טלפון בפינה. עברי בין:
- [ ] Desktop רחב (1920×1080)
- [ ] Desktop רגיל (1440×900)
- [ ] Tablet (iPad / 768×1024)
- [ ] Mobile (iPhone / 375×667)

### 📄 בדיקות תוכן בכל דף
לכל דף (`index`, `about`, `services-offered`, `industrial-consulting`, `eco-depot`, `partners`, `export-of-waste`, `types-of-waste-water`):
- [ ] כל התמונות נטענות (אין תיבה אדומה / איקס)
- [ ] כל הקישורים בתפריט עובדים
- [ ] כל הקישורים הפנימיים בדף עובדים
- [ ] טקסט בעברית מיושר ימין נכון
- [ ] התעודות (cleaning_certificate.html, release_certificate.html) נטענות נכון

### 🔍 בדיקות מהירות וביצועים
1. לכי ל-`https://pagespeed.web.dev/`.
2. הכניסי את `https://eco-oil.co.il`.
3. רוץ את הבדיקה.
4. **דווחי ללימור על הציון:**
   - 90+ = מצוין
   - 70-90 = טוב
   - מתחת ל-70 = יש מה לתקן (תמונות גדולות מדי לרוב)
5. **אם הציון מתחת ל-70**, הצעי ללימור: "כדאי לבדוק אילו תמונות הכי כבדות ולקטין אותן". אבל זה לא חוסם השקה.

### 🤖 בדיקות SEO בסיסיות
1. בכל דף — בדקי שיש תגית `<title>` ולא ברירת המחדל "Document".
2. בדקי שיש `<meta name="description">` עם תיאור הגיוני.
3. ודאי שיש `<meta charset="UTF-8">` בכל דף.

אם משהו חסר — **תיקון לפני שמכריזים על ההשקה.**

### 🌐 בדיקת שרידות (Robots & Sitemap)
1. **robots.txt** — קובץ פשוט בשורש האתר שאומר למנועי חיפוש מה לסרוק. צרי קובץ `website/robots.txt` עם תוכן בסיסי. **שאלי את לימור — לאשר?**
2. **sitemap.xml** — מפה של כל הדפים באתר עבור Google. אופציונלי לאתר קטן, אבל מומלץ.

### 📋 בדיקה אנושית — תני ללימור
- [ ] לימור פתחה את האתר בנייד שלה.
- [ ] לימור פתחה את האתר במחשב הפרטי שלה.
- [ ] לימור שלחה ליואב ו/או מישהו אחר את הקישור לבדיקה.
- [ ] חזרה אישור מהבודקים החיצוניים שהכל בסדר.

## אישור לפני המשך
- ✅ כל הבדיקות עברו.
- ✅ ליואב נשלח קישור והוא אישר.

---

# חלק 12: שלב 7 — מערכות עזר (אופציונלי בשלב הראשון)

## אנליטיקה — Google Analytics

מאפשר לראות כמה מבקרים יש באתר, מאיפה הם, אילו דפים פופולריים.

**שלבים בקצרה:**
1. לימור פותחת חשבון Google Analytics (`analytics.google.com`).
2. יוצרת "Property" עבור eco-oil.co.il.
3. מקבלת קוד מעקב (Measurement ID, נראה כמו `G-XXXXXXXXXX`).
4. אנחנו מוסיפים תגית `<script>` קטנה בכל דף `<head>`.

⚠️ **שאלי לימור אם רוצה את זה לפני שמתחילים** — זה מערב Google ושיתוף נתוני מבקרים.

## אימייל מקצועי (yoav@eco-oil.co.il)

זה תהליך נפרד — לא חלק מהעלאת האתר אלא תוסף שלה. יש שתי אופציות:

1. **Google Workspace** (היקר יותר, מומלץ) — אימייל באמצעות Google.
2. **שירות אימייל של Domain The Net** (זול, פחות מקצועי).

⚠️ **תהליך הזה מערב שינוי רשומות MX ב-DNS** — לא לעשות בלי הבנה מלאה.

**עצה ללימור:** לפני שמתחילות עם זה — לבדוק אם החברה כבר יש לה מערכת אימייל קיימת. אם כן — לא לגעת. אם לא — לתאם עם יואב.

## ניטור זמינות (Uptime Monitoring)

שירות חיצוני שבודק כל 5 דקות שהאתר באוויר ומתריע אם נופל. **חינמי בחבילות בסיסיות.**

מומלץ: `UptimeRobot.com` — חבילה חינמית מאפשרת ניטור של עד 50 אתרים.

---

# חלק 13: תחזוקה שוטפת

## איך לעשות שינויים באתר אחרי שהוא באוויר

זה הקסם של הארכיטקטורה שבחרנו:

1. **לימור או Claude** משנה משהו בקוד (במחשב העבודה).
2. עושים `git add`, `git commit`, `git push`.
3. **תוך דקה — Netlify מזהה את השינוי, פורס מחדש, ואת השינוי באוויר.**
4. אין צורך לעשות שום פעולה ידנית בנטליפיי או ב-Domain The Net.

## גיבויים

- **קוד האתר** — מגובה אוטומטית בגיט (GitHub).
- **פניות מהטפסים** — נשמרות ב-Netlify Forms (גם אחרי שנשלחו במייל).
- **הגדרות Netlify** — אפשר לייצא דרך CLI שלהם.

## תאריכי תפוגה לזכור

- **רישום הדומיין** — בדקי ב-Domain The Net כל שנה. **אם הדומיין יפוג — האתר נופל.**
  - מומלץ: להירשם ל-`auto-renew` (חידוש אוטומטי). לימור צריכה לוודא שיש כרטיס אשראי תקף בחשבון.
- **תעודת SSL** — Netlify מחדש אוטומטית. אין מה לעשות.
- **חשבון Netlify** — לא פג תוקף, אבל בדקי שאין מצב חיוב לא צפוי.

## עלות צפויה

לפי החבילה החינמית של Netlify:
- **תעבורה:** עד 100GB לחודש בחינם (לאתר של אקו אויל זה מספיק לשנים).
- **שמירת טפסים:** 100 פניות לחודש בחינם.
- **דומיין:** התשלום היחיד — חידוש שנתי ב-Domain The Net (כ-50-150 ₪ לשנה תלוי בסוג).

**סיכום עלות שנתית של האתר באוויר: כ-100 ₪ לשנה** (חידוש דומיין בלבד).

---

# חלק 14: בעיות נפוצות וטיפולים

## "האתר לא נטען בכלל"
- בדקי ב-`whatsmydns.net` שה-DNS עדכני.
- אם DNS לא עדכני — חכי עוד שעה-שעתיים, רענני.
- אם DNS עדכני — בדקי בדשבורד Netlify ש"Site is live".

## "המנעול בכתובת אדום / שגיאת תעודה"
- ב-Netlify → Domain Settings → HTTPS → לחצי "Renew Certificate".
- אם זה לא עובד תוך 10 דקות — דווחי ללימור והתחילו לבדוק עם תמיכת Netlify.

## "תמונה לא נטענת אבל זה אצלי במחשב נראה תקין"
- לרוב: **תווים בעברית בשם הקובץ.** השרתים של Netlify תומכים, אבל לפעמים יש בעיה. שני הצעדים לבדוק:
  1. בדפדפן בכתובת תמונה כתב מוזר? = בעיית קידוד.
  2. שם הקובץ במקור תואם בדיוק לכתיב ב-HTML (מקפה לעומת מקף תחתון, ועוד).

## "טופס נשלח אבל לא מקבלת מייל"
- בדקי **Spam folder**.
- בדקי שב-Netlify Notifications המייל הוגדר נכון.
- בדקי ש-Netlify Forms מזהה את הטופס: דשבורד → Forms → בדקי שהשליחה מופיעה ברשימה.

## "הדומיין מסרב לעבור ל-Netlify"
- בדקי שמחקת את כל רשומות A הישנות (לא רק הוספת חדשה).
- בדקי שאין רשומות סותרות (ANAME, ALIAS).
- אם תקועה — צרי קשר עם תמיכת Domain The Net (`*7820 או דרך האתר שלהם).

## "לימור עשתה משהו במחשב הפרטי שלה ועכשיו האתר באוויר נראה אחרת ממה שהיא רואה"
- כנראה: השינויים בלפטופ הפרטי לא נדחפו לגיט. צריך push.
- **חוק זהב:** "מקור האמת = הגיט. כל מה שלא בגיט — לא קיים."

---

# חלק 15: מה הלאה

## אחרי שהאתר באוויר — הצעדים הבאים

1. **חלוקת הקישור** — לימור ויואב יכולים להתחיל לחלק את הקישור ללקוחות, ספקים, רשתות חברתיות.

2. **הקמת מערכת הניהול** — הפרויקט שלנו כולל גם מערכת ניהול ב-Python/Flask (`eco_oil_platform/`). זה תהליך נפרד שיגיע **אחרי** שהאתר יציב באוויר.
   - הוסטינג למערכת הניהול: לא Netlify (לא תומך ב-Python). מומלץ Railway, Render, או DigitalOcean.
   - תת-דומיין מוצע: `admin.eco-oil.co.il` או `system.eco-oil.co.il`.

3. **הוספת שני פורטלי הלקוחות:**
   - **פורטל אקו אויל** — ללקוחות חברת אקו אויל.
   - **פורטל אקו דיפו** — ללקוחות אקו דיפו.
   - ייווצרו תחת תת-דומיינים נפרדים (`portal.eco-oil.co.il`, `depot.eco-oil.co.il`) או כתת-נתיבים (`/portal/oil`, `/portal/depot`).
   - יחוברו ל-API של מערכת הניהול.
   - **אלה לא קיימים באתר הנוכחי שעולה לאוויר.** כשתגיע השעה — נוסיף אותם.

4. **שיפורים ותחזוקה** — שינויים תוכן, עדכוני תמונות, פוסטים בלוג, וכו' — הכל באמצעות עדכון הקוד ו-push לגיט.

---

# 📎 נספח: רשימת תפעול לימור

מסמך מקוצר ללימור לעצמה — דברים שכדאי לזכור:

- **כתובת האתר:** `https://eco-oil.co.il`
- **כתובת הדומיין דה-נט:** `https://domainthenet.com`
- **כתובת Netlify:** `https://app.netlify.com`
- **כתובת GitHub:** `https://github.com/lio2egs-cmyk/eco_oil_platform`
- **דשבורד מהיר:** ב-Netlify יש לחצן "View site" ליד שם האתר — ילך ישר לאתר החי.
- **לראות מי ביקר:** Netlify → Analytics (יש קצת בחינם).
- **לראות פניות מטפסים:** Netlify → Forms → לחצי על שם הטופס.
- **לעדכן את האתר:** לבקש מ-Claude לעשות את השינוי, לאשר, היא תעשה commit + push, ובתוך דקה זה באוויר.

---

*עדכון אחרון: 2026-05-07*
*נכתב עבור: לימור טואג, פרויקט אקו אויל*
*נכתב על ידי: Claude Code (מופע מהמחשב הנייד הפרטי)*
